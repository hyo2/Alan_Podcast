# Railway에서 시스템 패키지 설치를 위한 Nixpacks 설정

[phases.setup]
nixPkgs = ["python313", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"