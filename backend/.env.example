# =========================================
# AI Audiobook 환경변수 템플릿 - .env.example
# =========================================

# ===== 환경 구분 =====
ENVIRONMENT=development

# ===== 내부 서비스 인증 =====
INTERNAL_SERVICE_TOKEN=your-secret-token-here

# ===== 서버 설정 =====
PORT=4001
LOG_LEVEL=INFO

# ===== 인증 서버 설정 =====
# 인증 모드: mock | alan
AUTH_MODE=mock

# Alan Auth Server Base URL
ALAN_AUTH_BASE_URL=

# 접근 정책: pro_only | all
ACCESS_POLICY=pro_only

# Pro로 간주하는 Role 목록 (신규 Role 추가 대비)
PRO_ROLE_ALLOWLIST=pro_user,internal_user,pro_user_promotion

# ===== CORS 설정 =====
# development: 빈 값 (코드에서 * 사용)
# staging/production: 허용할 origin (콤마 구분)
CORS_ORIGINS=

# ===== Vertex AI (Google Cloud) =====
VERTEX_AI_PROJECT_ID=your-project-id
VERTEX_AI_REGION=us-central1
VERTEX_AI_SERVICE_ACCOUNT_FILE=path/to/service-account.json
VERTEX_AI_MODEL_TEXT=gemini-2.5-flash

# Gemini OCR 용
GEMINI_OCR_FALLBACK=true
GEMINI_OCR_MAX_SAMPLE_PAGES=15

# ===== 데이터베이스 =====
# PostgreSQL 연결
DATABASE_URL=postgresql://user:password@host:5432/database
# 저장소 백엔드 - memory/postgres
REPO_BACKEND=postgres

# ===== 스토리지 =====
# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=your-connection-string
AZURE_STORAGE_CONTAINER=alan-files

# Azure Storage Queue
AZURE_STORAGE_QUEUE_NAME=ai-audiobook-jobs

# ===== Azure Functions 설정 =====
FUNCTIONS_WORKER_RUNTIME=python
AzureWebJobsStorage=your-connection-string

# 스토리지 백엔드 - azure/local
STORAGE_BACKEND=azure

# 출력 디렉토리 (로컬 모드)
BASE_OUTPUT_DIR=./outputs

# ===== 가격 설정 (USD 기준) =====
PRICING_LLM_INPUT=0.30
PRICING_LLM_OUTPUT=2.50
PRICING_VISION=0.30
PRICING_TTS=16.00
PRICING_STT=0.96
EXCHANGE_RATE_KRW=1462.20

# ===== LangSmith 추적 (선택) =====
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=ai-audiobook
LANGSMITH_TRACING=false
LANGCHAIN_TRACING_V2=false